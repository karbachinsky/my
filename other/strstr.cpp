#include <cassert>
#include <iterator>


std::string::iterator strstr(const std::srting & pattern, const std::string &str) {

}


int main() {
    std::string::iterator it;
    str::string pattern("abbababa");
    std::string str("bab");

    it = strstr(pattern, str); 

    assert(std::distance(pattern.begin(), ir) == 3);
}
